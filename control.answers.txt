1.-язык программирования в котором есть ограничения на приведение типов в присваиваниях и операциях
2.в памяти c# есть области памяти ,например stack(временные данные,переменные) и heap(динамически созданные обЪекты)
  так же есть сборщик мусора,удаляющий объекты на которые нет ссылок
3.1.типы значений,stack пример int,double,bool
  2.типы ссылок ,heap пример string
  3.указатели
  4.структуры
  5.перечисления
4.неявное double x = 3.5;
          int y = x;
  явное преобразование  int x = 35;
                        double y = (double)x;
5.   int i = 1;
     object x= i;
     x = 1;
     int j = (int)x;
6.
  & оценивает несколько операнд,и даже если левая не правильная все равно продолжает пример int a=1;int b=2;(a==3&b==2)//продолжит работу
  && оценивает несколько операнд,если левая не правильная прекращает работу int a =1;int b=2;(a==3&&b==2)//не продолжит работу
7.
  while сначала проверяет условие,а затем выполняет цикл
  do while сначала выполняет цикл,затем проверяет условие,и если оно верное выполняет цикл 
8. массив это упорядоченный набор элементов одного типа
  массивы бывают одномерные,многомерные и зубчатые
9.класс шаблон описывающий состояние и поведение
  объект конкретный экземпляр класса
10.метод использующийся для инициализации объекта
11.
12.не поддерживет
13.1.private доступен только в рамках своего класса
   2.private protected доступен из любого места своего класса
   3.internal доступны из любого места кода,но не доступны для других программ
   4.protected internal совмещает protected и internal
   5.public доступен везде
14.
15.один из принципов ООП,нужна для сокрытия данных
16.abstract не имеет реализации в базовом классе, объявляется словом abstract
   virtual  имеет реализацию по умолчанию ,объявляется словом virtual
17.класс члены которого наследуются
18.это класс,который не позволяет создавать объекты напрямую
19.применяется при создании экземпляров классов и массивов
20.можно использовать ключевое слово sealed